[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Disable warnings for dependencies. tests/conftest.py configures environment
# variables before importing the application, without relying on pytest-env.
filterwarnings =
    ignore::DeprecationWarning
